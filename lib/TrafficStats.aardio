

//流量统计
import xray.core;
import config;

namespace TrafficStats;
coreType = "xray";
corePath = ..io.fullpath("/core/xray/xray.exe");


var inbounds = ..config.core.default[["inbounds"]];
var apiPort = inbounds[3].port;
//apiPort = 10084;

import debug
import debug.log
//import debug.hook()
import console
//console.print( apiPort)
//debug.debug()

reset = function(){
	import debug
	import debug.log
	//debug.log.print("reset函数启动")
	if( prcsCmd ){ prcsCmd.terminate();prcsCmd = null; }
	import xray.core;

	port = '--server=127.0.0.1:'+apiPort;
	//coreType = ..Core.XrayCore.coreType;
	
	getPath = function(){
		import debug
	import debug.log
	debug.log.print("reset函数0")
	var path = ..io.fullpath("/core/xray/xray.exe");
	if(..io.exist(path)){
		return path;
	}
	debug.log.print("reset函数1")
	coreType = "xray";
	corePath = getPath();
	corePath = ..io.fullpath("/core/xray/xray.exe");
	debug.log.print("corePath"+corePath);
	}
}
reset();

start = function(){
	import debug
	import debug.log
	//import config
	//debug.log.print("流量监控启动")
    //reset();
    corePath = ..io.fullpath("/core/xray/xray.exe");
    //debug.log.print("corePath路径:"+corePath);
    if(!corePath || !coreType){ return }
    //debug.log.print("流量监控1")
					
	if(coreType=="xray"){
		//【性能】改为纯 gRPC 直连读取统计（xray.app.stats.command.StatsService/GetStats）。
		//
		//原来这里是 process.popen(corePath,"api","statsquery",...)：
		//每次都要「启动一个新的 xray.exe 进程 → 连 API → 读输出 → 退出」，
		//实测单次 500~610 毫秒；而它被 2 秒一次的流量统计定时器调用
		//（forms/main/xray.aardio 的 frmXray.trafficStats，apiTime=2000）
		//→ 界面线程每 2 秒被占住半秒以上，这是主界面卡顿的主要来源。
		//
		//gRPC 直连（lib/xray/grpc.aardio，纯 aardio 实现 h2c + gRPC + protobuf）：
		//实测「建连 + 读下行 + 读上行」共约 12 毫秒，比进程方案快约 50 倍，
		//也不再需要 win.invoke 额外创建一个界面线程（线程创建本身还要约 40ms）。
		//
		//reset 参数与原来的 "-reset" 等价：读取后清零，返回的是「本周期增量」。
		import xray.grpc;

		var client = ..xray.grpc.create("127.0.0.1",apiPort,3000);

		var down = ..xray.grpc.readStat(client,"outbound>>>proxy>>>traffic>>>downlink",true);
		var up = ..xray.grpc.readStat(client,"outbound>>>proxy>>>traffic>>>uplink",true);

		//读取失败（内核未就绪 / API 端口未监听）时返回 0：
		//调用方用 if(downValue) 判断，0 表示「本周期无增量」，不会误加流量，也不会中断定时器。
		if(down === null) down = 0;
		if(up === null) up = 0;

		return down,up;
	}
	elseif(coreType=="v2ray"){
				debug.log.print("流量监控3")
		corePath = ..io.joinpath(..io.splitpath(corePath).dir,"v2ctl.exe");
		var downTraffic = ..win.invoke(
			function(corePath,apiPort){
				import process.popen;
				import web.json;
				var cmdText = '"outbound>>>proxy-out>>>traffic>>>downlink"';
				
				var json ;
				var i = 3 ; 
				do {
					prcsCmd = ..process.popen(corePath,corePath,"api",apiPort,"StatsService.GetStats",'"name:'+cmdText+'reset:true"');
					var json = string.replace(string.replace(prcsCmd.read(-1),"\<",'\{'),"\>\s*$",'\}');
					i--;
				}while( !json && i > 0)
				var table = ..web.json.tryParse(json);
				var value = (json && json.stat.value) ? json.stat.value : false;
				return value; 
			}
		)
		var upTraffic = ..win.invoke(
			function(corePath,apiPort){
				import process.popen;
				import web.json;
				var cmdText = '"outbound>>>proxy-out>>>traffic>>>uplink"';
				prcsCmd = ..process.popen(corePath,corePath,"api",apiPort,"StatsService.GetStats",'"name:'+cmdText+'reset:true"');
				var json = string.replace(string.replace(prcsCmd.read(-1),"\<",'\{'),"\>$",'\}');
				var table = ..web.json.tryParse(json);
				var value = (json && json.stat) ? json.stat.value : false;
				return value; 
			} 
		)
		return downTraffic,upTraffic;
	}
}

/**intellisense(TrafficStats)
reset = 重置
start = 开始流量统计
end intellisense**/
